# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'swiftChatTest' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for swiftChatTest
  pod 'Firebase'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'

  target 'swiftChatTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'swiftChatTestUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
